# 002 — nvs_store

## Goal
Persist runtime config (MQTT broker, fallback AP) and N Somfy virtual remotes (`remote_id` 24-bit + `rolling_code` 16-bit + name) in NVS.

## Target modules
`include/nvs_store.h`, `src/nvs_store.cpp`

## Status
Backlog — full spec / architecture / plan to be written at iteration start.
